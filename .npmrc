registry=https://registry.npmjs.org/

# Link workspace packages locally when their version satisfies a dependency's
# semver range, so in-repo packages build against local source instead of the
# published registry copy. This does NOT rewrite published manifests (unlike the
# `workspace:` protocol), so plain-semver deps stay npm-publish compatible.
link-workspace-packages=true
prefer-workspace-packages=true
